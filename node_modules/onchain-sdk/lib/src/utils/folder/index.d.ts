import { APP } from '../../typings';
export declare enum FolderType {
    /** 实例 */
    Instance = 2,
    /** 文件夹 */
    Folder = 1
}
declare class FolderBasicsUtils {
    /** 是否是实例文件 */
    static isInstance(item: APP.instanceInfoType | Record<string, any>): boolean;
    /** 是否是文件夹 */
    static isFolder(item: APP.instanceInfoType | Record<string, any>): boolean;
    /** 是否是文件夹 */
    static getServerType(item: APP.instanceInfoType | Record<string, any>): FolderType;
}
export default FolderBasicsUtils;
