import { UserIdentity } from '../../utils/userIdentity';
declare const useUserIdentity: () => {
    updateUserIdentity: () => Promise<UserIdentity>;
    userIdentity: UserIdentity | undefined;
};
export default useUserIdentity;
