/// <reference types="react" />
import { PlmTableColumnProps } from '../../components/PlmTable';
import { tabInfoProps } from '../../components/PlmTabTemplate';
import { APP } from '../../typings';
import { BasicsAttribute } from '../../utils/attribute';
import { ColumnRenderOption, GenerateColumnsProps, GenerateRenderExtra, MapManagement } from '../../utils/columns';
import { ProjectPublicAttribute, ProjectSubset } from '../../utils/project/const';
export declare function useProjectColumns({ showColumns, instanceInfo, tabInfo, isNew, generate, type, renderOption, insert, sorter, }: {
    type: ProjectSubset;
    instanceInfo: APP.ProjectInstance;
    tabInfo: tabInfoProps;
    showColumns: ProjectPublicAttribute[];
    isNew?: boolean;
    generate?: GenerateColumnsProps<ProjectPublicAttribute>['generate'];
    renderOption?: ColumnRenderOption<GenerateRenderExtra, ProjectPublicAttribute>[];
    insert?: PlmTableColumnProps;
    sorter?: (attr: BasicsAttribute) => void;
}): {
    loading: boolean;
    map: import("react").MutableRefObject<MapManagement<{
        colApiCodeIdMap: Record<string, any>;
        colApiCodeDataFromIdMap: Record<string, any>;
        listCodeMap: Record<string, any>;
    }>>;
    columns: PlmTableColumnProps;
};
