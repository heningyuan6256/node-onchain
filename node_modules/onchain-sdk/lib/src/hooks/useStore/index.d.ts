/**
 * @description 多状态管理
 * @date 2023-04-18 10:37:49
 * @export
 * @template T
 * @param {T} initStore 接收一个对象
 */
declare function useStore<T>(initStore: T): {
    store: T;
    setStore: (params: Partial<T> | ((store: T) => T | void)) => void;
    resetStore: () => void;
};
export default useStore;
