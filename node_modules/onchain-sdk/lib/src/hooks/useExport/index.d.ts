import { APP } from '../../typings';
import { TabInfo } from '../useTabInfo/type';
declare const useExport: ({ instanceInfo, tabInfo, changeInstance, bomLevel, viewId, }: {
    instanceInfo: APP.instanceInfoType;
    tabInfo: TabInfo;
    changeInstance?: APP.instanceInfoType<string> | undefined;
    bomLevel?: any;
    viewId?: string | undefined;
}) => {
    exportLoading: boolean;
    runExport: () => Promise<void>;
};
export default useExport;
