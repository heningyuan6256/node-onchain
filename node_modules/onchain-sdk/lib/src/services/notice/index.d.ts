import { ResponseData } from '../../typings';
/**
 * 通知
 */
export default class NoticeService {
    static getList(data: any): Promise<ResponseData>;
    static noticeEdit(data: any): Promise<ResponseData>;
    static getCount(): Promise<ResponseData>;
    static noticeSave(data: any): Promise<ResponseData>;
}
