import { ResponseData } from '../../typings';
interface AddParams {
    /** 视图名称 */
    name: string;
    /** 是否默认 */
    isDefault: boolean;
    itemCode: string | number;
    tabCode: string | number;
    /** 范围  0全局 1个人，普通用户只能添加修改个人，管理员可添加修改全局 */
    scope: '0' | '1';
    /** 适应分类ids */
    objectIds: string;
    /** 适应分类名称 ('任务,项目') */
    objectNames?: string;
}
export default class ColumnsConfigService {
    static customViewList(data: {
        itemCode: string | number;
        tabCode: string | number;
        objectId?: string | number;
        sort?: string | number;
    }): Promise<ResponseData>;
    static addViewList(data: {
        /** 视图名称 */
        name: string;
        /** 是否默认 */
        isDefault: boolean;
        itemCode: string | number;
        tabCode: string | number;
        /** 范围  0全局 1个人，普通用户只能添加修改个人，管理员可添加修改全局 */
        scope: '0' | '1';
        /** 适应分类ids */
        objectIds: string;
        /** 适应分类名称 ('任务,项目') */
        objectNames: string;
    }): Promise<ResponseData>;
    static editViewList(data: AddParams & {
        id: string;
    }): Promise<ResponseData>;
    static viewSaveAs(data: {
        id: string;
        /** 视图名称 */
        name: string;
        /** 是否默认 */
        isDefault: boolean;
        itemCode: string | number;
        tabCode: string | number;
        /** 范围  0全局 1个人，普通用户只能添加修改个人，管理员可添加修改全局 */
        scope: '0' | '1';
        /** 适应分类ids */
        objectIds: string;
        /** 适应分类名称 ('任务,项目') */
        objectNames: string;
    }): Promise<ResponseData>;
    static deleteViewList(ids: string[]): Promise<ResponseData>;
    static customViewDetail(id: string): Promise<ResponseData>;
    static customViewTabColumns(data: {
        tabCode: string | number;
        itemCode: string | number;
    }): Promise<ResponseData>;
    static customViewColumSave(data: {
        viewId: string;
        attrId: string;
        sort: number;
    }[]): Promise<ResponseData>;
    static getTreeListByItemCode(data: {
        tabCode: string | number;
        itemCode: string | number;
    }): Promise<ResponseData>;
}
export {};
