import React from 'react';

export interface TableFooterDataRender {
  (params: {
    tableDataLen: number;
    total: number;
    selectedLen: number;
    boxStyles: React.CSSProperties;
    refreshNode: React.ReactNode;
  }): React.ReactNode;
}
