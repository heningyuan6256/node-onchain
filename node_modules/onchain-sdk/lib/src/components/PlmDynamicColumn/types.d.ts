import { singleColumnType } from '../PlmTable';
export type Columns = (singleColumnType & { apicode?: string; show?: boolean; custom?: boolean; sort?: number })[];
export type PlmDynamicColumn = Columns[0];
