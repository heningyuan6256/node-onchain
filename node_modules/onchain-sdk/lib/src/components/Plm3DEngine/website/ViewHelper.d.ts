import * as THREE from 'three';
export declare class ViewHelper extends THREE.Object3D {
    editorCamera: THREE.Camera;
    private readonly container;
    private readonly panel;
    private readonly point;
    private readonly dim;
    private readonly posXAxisHelper;
    private readonly posYAxisHelper;
    private readonly posZAxisHelper;
    private readonly negXAxisHelper;
    private readonly negYAxisHelper;
    private readonly negZAxisHelper;
    private readonly camera;
    private readonly interactiveObjects;
    private readonly raycaster;
    private readonly mouse;
    constructor(editorCamera: THREE.Camera, container: HTMLElement);
    render(renderer: THREE.WebGLRenderer): void;
    handleClick(event: MouseEvent): void;
}
