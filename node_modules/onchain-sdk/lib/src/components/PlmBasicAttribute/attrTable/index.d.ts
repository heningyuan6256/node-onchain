import { BasicsAttribute } from '../../../utils/attribute';
import React from 'react';
interface Props {
    instanceInfo: any;
    handleSubmit: (fromValue: Record<string, any>, attr?: BasicsAttribute[]) => Promise<any>;
}
declare const AttrTable: React.FC<Props>;
export default AttrTable;
