/**
 * @Description 所有预览要经过的布局layout
 * 接受除了文件url需要各自的参数
 * 各自url需要接收相应的后缀名称
 * 其中3D预览需要接受load作为文件url
 * 其中2D预览需要接受file作文文件url
 * 其中xlsx预览需要接受file作文文件url
 * 还需要接受的有文件格式，文件名称，文件大小分别为fileName, fileType，fileSize
 * @Author heningyuan
 */
/// <reference types="react" />
export declare const judgeViewer: (type: any, formats?: any[]) => boolean;
export declare const downloadProgress: (response: any, downBlob: any) => void;
declare const PlmFilePreview: React.FC;
export default PlmFilePreview;
