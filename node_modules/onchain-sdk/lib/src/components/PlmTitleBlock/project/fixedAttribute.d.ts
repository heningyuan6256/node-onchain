import { Props } from './index';
import { APP } from '../../../typings';
import { ProjectPublicAttribute, ProjectSubset } from '../../../utils/project/const';
interface EleProps extends Props {
    isProjectChange: boolean;
    info: APP.ProjectInstance;
    initialState: any;
}
interface FixedAttribute {
    apicode: ProjectPublicAttribute;
    ele?: (data: EleProps) => JSX.Element;
}
export declare const showFixedAttr: Record<ProjectSubset, FixedAttribute[]>;
export {};
