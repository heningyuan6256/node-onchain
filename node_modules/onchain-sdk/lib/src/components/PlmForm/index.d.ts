import { BaseComponentProps } from '../../typings';
import { FormProps } from 'antd';
import { FormInstance } from 'antd';
import { FC } from 'react';
export interface PlmFormProps extends BaseComponentProps, FormProps {
    readOnly?: boolean;
    name?: string;
    height?: number;
}
export interface PlmFormForwardRefProps extends FormInstance {
}
declare const PlmForm: FC<PlmFormProps>;
export default PlmForm;
