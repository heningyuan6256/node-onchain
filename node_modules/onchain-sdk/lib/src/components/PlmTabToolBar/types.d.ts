export interface ButtonConfig {
  code: string;
  enabled: boolean;
  icon: string;
  id: string;
  location: 'left' | 'right';
  name: string;
  show: boolean;
  sort: number;
  /** 禁用提示 */
  disabledTips?: string;
  /** 后台图标 */
  backend?: boolean;
  [k: string]: any;
}
