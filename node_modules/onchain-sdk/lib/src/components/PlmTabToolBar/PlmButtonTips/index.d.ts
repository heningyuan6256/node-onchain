import { TooltipProps } from 'antd';
import React from 'react';
import { ButtonConfig } from '../types';
declare const PlmButtonTips: React.FC<{
    config?: ButtonConfig;
} & Omit<TooltipProps, 'title'>>;
export default PlmButtonTips;
