import { BaseComponentProps } from '../../typings';
import React from 'react';
export interface toolButtonProps {
    icon: string | Function;
    name: string;
    id: string;
    color: string;
    loading: boolean;
    isBackend?: boolean;
    render?: (btn: React.ReactNode, item: toolButtonProps) => React.ReactNode;
    disabled?: boolean;
}
export interface PlmTabToolBarProps extends BaseComponentProps {
    buttonList: toolButtonProps[];
    onClick?: (name: string, item: any) => void;
    isRadio?: Boolean;
    selectedNum?: number;
    tab?: string;
    selectRows?: any;
    propsObj?: any;
    serveColummsData?: any;
    btnPurview?: any;
    onDisabled?: (item: toolButtonProps) => boolean;
    isDisabled?: boolean;
    isPreview?: boolean;
    hasAuth?: boolean;
    activeStatus?: Record<string, boolean>;
    total?: number;
    /**
     * 用于判断额外的按钮权限，坐覆盖
     */
    extraBtnStatusMap?: {
        value: string;
        num: 1 | 2;
    };
}
declare const PlmTabToolBar: React.FC<PlmTabToolBarProps>;
export default PlmTabToolBar;
