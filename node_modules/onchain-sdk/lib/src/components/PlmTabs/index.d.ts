import React from 'react';
import { BaseComponentProps } from '../../typings';
export interface Panes {
    tab: string | React.ReactNode;
    tabKey: string | number;
}
export interface PlmInstanceTabsProps extends BaseComponentProps {
    panes: Panes[];
    loading?: boolean;
    defaultActiveKey?: string;
    onTabClick?: (key: string) => boolean | void;
}
export interface PlmInstanceTabsRef<T = string> {
    active: (key: T) => void;
}
declare const PlmTabs: React.FC<PlmInstanceTabsProps>;
export default PlmTabs;
