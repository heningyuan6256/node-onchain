import { tabInfoProps } from '../../PlmTabTemplate';
import { APP } from '../../../typings';
import React from 'react';
interface Props {
    instance: APP.instanceInfoType;
    pageInstance: APP.instanceInfoType;
    tabInfo: tabInfoProps;
    tabHeight?: number;
    isPreview?: boolean;
    /** 是否是小窗口布局 */
    isDwarfLayout?: boolean;
    setProjectInstance?: (instance: APP.instanceInfoType) => void;
    onRefresh?: () => void;
}
declare const EffectPlan: React.FC<Props>;
export default EffectPlan;
