import React from 'react';
import { PlmTabTemplateProps } from '../../PlmTabTemplate';
import { APP } from '../../../typings';
type Task = PlmProject.NoLimitTask;
interface Props {
    topInst: APP.ProjectInstance;
    changeInst?: APP.instanceInfoType;
    isProjectChange?: boolean;
    onEffectRefresh?: () => void;
    onAttrUpdate: (attr?: Record<string, any>) => Promise<any>;
}
export interface InstDetailsTaskRef {
    /** tabKey (1:概览 | 2:目标交付 | 3:交付清单 | 4:资源 ) */
    open: (task: Task, tabKey: '1' | '2' | '3' | '4', onSave?: PlmTabTemplateProps['onSave']) => void;
}
export declare function globalOpenInstDetails(task: Task): void;
declare const InstDetails: React.ForwardRefExoticComponent<Props & React.RefAttributes<InstDetailsTaskRef>>;
export default InstDetails;
