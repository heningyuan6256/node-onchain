import React from 'react';
import { GanttStatic } from 'dhtmlx-gantt';
import { OperationBarProps } from '..';
import { PlmTask, Rel } from '../../utils/type';
type Task = PlmTask;
interface Props {
    gantt?: GanttStatic;
    onUpdate: OperationBarProps['tableHandle']['onUpdate'];
    onAddSuccess?: (tasks: Task[]) => Promise<any>;
}
export interface AddTaskRef {
    open: (task: Rel, selectedKeys?: string[], prioritySaveUpdate?: () => Promise<void>) => Promise<any>;
}
declare const _default: React.MemoExoticComponent<React.ForwardRefExoticComponent<Props & React.RefAttributes<AddTaskRef>>>;
export default _default;
