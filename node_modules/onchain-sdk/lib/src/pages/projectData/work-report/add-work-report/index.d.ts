import React from 'react';
import { APP } from '../../../../typings';
type Open = (fn: (values: Record<string, any>) => void, instance?: Record<string, any>) => void;
type Close = () => void;
export interface AddWorkReportRef {
    open: Open;
    close: Close;
}
export interface Injection {
    instance: APP.ProjectInstance;
}
interface Props extends Injection {
}
declare const AddWorkReport: React.ForwardRefExoticComponent<Props & React.RefAttributes<AddWorkReportRef>>;
export default AddWorkReport;
