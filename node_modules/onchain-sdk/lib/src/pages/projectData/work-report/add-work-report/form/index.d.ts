import { FormInstance } from 'antd/lib/form/Form';
import React from 'react';
import { Injection } from '..';
import { BasicsAttribute } from '../../../../../utils/attribute';
interface Props extends Injection {
    visible: boolean;
    attrs: BasicsAttribute[];
}
export interface FormRef {
    getForm: () => FormInstance;
    getUserInfo: (ids: string[]) => Record<string, any>[];
}
declare const Form: React.ForwardRefExoticComponent<Props & React.RefAttributes<FormRef>>;
export default Form;
