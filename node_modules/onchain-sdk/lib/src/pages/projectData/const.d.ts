import { TabInfo } from '../../hooks/useTabInfo/type';
import { APP } from '../../typings';
export declare const projectPageCache: {
    targetDeliveryCount: Map<string, string>;
};
export declare const getTaskDeliverFn: CacheFn<Promise<any>, {
    itemCode: string[];
    objectIds: string[];
}>;
export declare function getAddDeliverInfo({ getTaskDeliver, newData, tabInfo, extraJudgment, }: {
    tabInfo: TabInfo;
    newData: APP.instanceInfoType[];
    extraJudgment?: boolean;
    getTaskDeliver?: () => Promise<any>;
}): Promise<{
    is: boolean;
    data: APP.instanceInfoType<string>[];
    save: APP.instanceInfoType<string>[];
    cannotSave: APP.instanceInfoType<string>[];
}>;
