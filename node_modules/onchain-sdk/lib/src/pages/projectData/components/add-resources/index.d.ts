import React from 'react';
import { OnOpenUtilization } from '../const';
interface Props {
    onOpenUtilization?: OnOpenUtilization;
    tableData?: any[];
    originTableColumns: Record<string, any>[];
}
export interface AddResourcesModalRef {
    open: (props: {
        instance: Record<string, any>;
    }) => void;
    setOnSubmit: (fn: (formValues: Record<string, string>) => Promise<any>) => void;
}
declare const AddResourcesModal: React.ForwardRefExoticComponent<Props & React.RefAttributes<AddResourcesModalRef>>;
export default AddResourcesModal;
