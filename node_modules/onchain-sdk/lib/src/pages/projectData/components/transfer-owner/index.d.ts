import React from 'react';
import { APP } from '../../../../typings';
interface Props {
    /** @deprecated 是否阻止所有者转移后的实例刷 (例如：onSuccess 内会刷新实例，避免调2次，需要把`blockMessageRefresh`设置为`true`) */
    blockMessageRefresh?: boolean;
    onSuccess?: () => void;
}
export interface TransferOwnerRef {
    open: (props: {
        instance: APP.instanceInfoType;
        user?: Record<string, any>;
        selectRows?: APP.instanceInfoType[];
        isResources?: boolean;
    }) => void;
}
declare const TransferOwnerModal: React.ForwardRefExoticComponent<Props & React.RefAttributes<TransferOwnerRef>>;
export default TransferOwnerModal;
