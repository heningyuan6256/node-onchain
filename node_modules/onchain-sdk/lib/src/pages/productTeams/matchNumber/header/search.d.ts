import React from 'react';
import { Row } from '../match/type';
interface Props {
    onAdd?: (rows: Row[]) => void;
    isMultiple?: boolean;
    getExistRow?: () => Row[];
}
export interface MaterielRef {
    open: () => Promise<any>;
    close: () => void;
}
declare const MaterielModal: React.ForwardRefExoticComponent<Props & React.RefAttributes<MaterielRef>>;
export default MaterielModal;
