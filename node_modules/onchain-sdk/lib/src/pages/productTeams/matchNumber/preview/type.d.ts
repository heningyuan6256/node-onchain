import { ButtonConfig } from '../../../../components/PlmTabToolBar/types';
import { Row } from '../match/type';

export interface Store {
  ID: string;
  loading: boolean;
  dialogVisible: boolean;
  isReplaceDialog: boolean;
  selects: any[];
  dataSource: Row[];
  expandedRowKeys: any[];
  noAccessBts: ButtonConfig[];
}
