import { HomeTabKey } from '../../../components/PlmHomeLayout/utils';
import { BadgeButton } from '../tab/utils';

export interface PieConfig {
  id: string;
  name: string;
  value: any;
  tableKey: HomeTabKey;
  badgeButton: BadgeButton;
}
