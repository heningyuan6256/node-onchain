import { HomeTabKey, Tab } from '../../../components/PlmHomeLayout/utils';
import type { TransferOwnerRef } from '../../projectData/components/transfer-owner';
import { PlmTableProps } from '../../../components/PlmTable';
import { APP } from '../../../typings';
import { AddWorkReportRef } from '../../projectData/work-report/add-work-report';

export interface HomeBasicsTableRef {
  refresh: () => void;
  namespace: string;
}

export interface HomeBasicsTableProps {
  tabActiveKey: HomeTabKey;
  tab: Tab;
  tableProps?: Partial<PlmTableProps>;
  title?: string;
  /** 当前页面相邻元素的ref */
  adjoinElementRef?: React.MutableRefObject<Record<string, HomeBasicsTableRef>>;
  loading?: boolean;
}

export interface HomePageContextValue {
  transferOwner: React.RefObject<TransferOwnerRef>;
  tabSkip: Tab | null;
  updateAllTabCount: () => void;
  viewInstances: React.Dispatch<any>;
  setTableNamespaceActive: (value: string) => void;
  addWorkReportRef: React.RefObject<AddWorkReportRef>;
  setSelectInstanceInfo: (value: APP.Workflow[]) => void;
  closeApprovalModal: () => void;
}
