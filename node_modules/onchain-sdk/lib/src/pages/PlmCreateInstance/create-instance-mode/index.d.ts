import React from 'react';
interface CreateInstanceModeProps {
    onChange: (e: string) => void;
    defaultSelected?: string;
}
declare const CreateInstanceMode: React.FC<CreateInstanceModeProps>;
export default CreateInstanceMode;
