import { ProductLineType } from './const';

/** 产线对象 */
export interface ProductLineInfo {
  id: string;
  name: string;
  type: ProductLineType.depart | ProductLineType.productLine | ProductLineType.spectrum | string;
  leaf: boolean;
  status?: string;
  parentId?: string;
  personInCharge?: string;
  children?: ProductLineInfo[];
  sort?: number;
  [k: string]: any;
}
